/*
const person = {
    firstname: "ravi",
    lastname: 'shukla',
    city: 'kanpur',
    fullname: function(city) { // arrow function we can't use in object methods
        console.log(`${this.firstname} ${this.lastname} ${city}`)
    }
}

person.fullname();

const person1 = {
    firstname: "kavi",
    lastname: 'singh',
}

person.fullname.call(person1, 'lko'); // a object borrow the function and method of another object by using call keyword
person.fullname.apply(person1, ['pkl']) // same as call but we pass argunment in array
bind used as same as call
*/

